---
project_name: prometheus-config-controller

release:
  github:
    owner: dbsystel
    name: prometheus-config-controller

builds:
  - binary: prometheus-config-controller
    goos:
      - darwin
      - windows
      - linux
    goarch:
      - amd64
      - 386
    env:
      - CGO_ENABLED=0
    main: ./cmd/
    ldflags: -s -w -X main.version={{.Version}} -X main.commit={{.ShortCommit}} -X main.date={{.Date}}

archive:
  format: tar.gz
  wrap_in_directory: true
  format_overrides:
    - goos: windows
      format: zip
  name_template: '{{ .Binary }}-{{ .Version }}-{{ .Os }}-{{ .Arch }}{{ if .Arm }}v{{ .Arm }}{{ end }}'
  files:
    - LICENSE
    - README.md

snapshot:
  name_template: SNAPSHOT-{{ .Commit }}

checksum:
  name_template: '{{ .ProjectName }}-{{ .Version }}-checksums.txt'

changelog:
  sort: asc
  filters:
    exclude:
    - '^docs:'
    - '^test:'
    - '^dev:'
    - 'README'
    - Merge pull request
    - Merge branch

dockers:
  - image_templates:
      - "dbsystel/prometheus-config-controller:latest"
      - "dbsystel/prometheus-config-controller:{{ .Tag }}"
      - "dbsystel/prometheus-config-controller:{{ .Major }}.{{ .Minor }}"

